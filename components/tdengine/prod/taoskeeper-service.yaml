apiVersion: v1
kind: Service
metadata:
  name: taoskeeper-prod
  namespace: tdengine
  labels:
    app: tdengine-prod
    role: taoskeeper
spec:
  ports:
    - name: http
      port: 6043
      targetPort: 6043
  selector:
    app: tdengine-prod
    role: taoskeeper
  type: ClusterIP
