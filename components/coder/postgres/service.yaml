apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: coder
  labels:
    app: coder-postgres
spec:
  type: ClusterIP
  selector:
    app: coder-postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
