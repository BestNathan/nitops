apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coder-nfs
  namespace: coder
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: coder-nfs
