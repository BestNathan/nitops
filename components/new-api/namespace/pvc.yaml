apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: new-api-nfs
  namespace: new-api
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  volumeName: new-api-nfs
